import React from 'react'

const List = (props) => {

    const ListarFinanzas = (event) => (event.target.value); 


    <button onClick={ListarFinanzas}>Añadir a la lista</button>

}

export default List
